# 复制本文件为 .env 并填入你的真实密钥（.env 已被 git 忽略，不会提交）
# Moonshot / Kimi 的 API Key，获取地址：https://platform.moonshot.cn/
MOONSHOT_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---- Langfuse tracing（可选）----
# 不填这几项也能正常跑 agent，只是不记录 trace（tracing 静默 no-op）。
# 想看交互 log 时：先 docker compose up -d 起本地 Langfuse，
# 打开 http://localhost:3000 注册并建一个项目，在 Settings → API Keys 里生成密钥后填到下面。
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 自托管地址；用 docker-compose.yml 起的默认就是这个
LANGFUSE_BASE_URL=http://localhost:3000
